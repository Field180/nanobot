"""
services/ — Pure business-logic functions extracted from server_final.py (P15).

Each module exposes plain functions that accept explicit parameters and return
dicts, with no dependency on FastAPI Request/Response objects.
"""
